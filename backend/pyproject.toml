[project]
name = "backend"
version = "0.1.0"
description = "Seal – personal AI assistant backend"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "vercel>=0.5.9",
    "vercel-workers>=0.0.22",
    "asyncpg>=0.29.0",
    "httpx>=0.28.1",
    "ai",
]

[tool.mypy]
python_version = "3.13"
strict = true
plugins = ["pydantic.mypy"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "standard"
reportMissingTypeStubs = false
reportUnusedCallResult = false

[tool.ruff]
target-version = "py313"
src = ["."]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.uv.sources]
ai = { git = "https://github.com/vercel-labs/ai-python", rev = "refactor-ui-adapter" }

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pydantic>=2.12.5",
    "pyright>=1.1.400",
    "pytest>=9.0.2",
    "ruff>=0.15.5",
]
